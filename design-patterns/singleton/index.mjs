import { singleton } from "./singleton.mjs";

singleton.output("Hello, world!");


/**
 * so if you point the terminal to singleton folder and then type node index.mjs 
 * it will show the console log value message we set, as we are calling the output method 
 * in singleton that is created as a new istance of Singleton 
 */
